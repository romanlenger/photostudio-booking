# 📸 Система Бронювання Фотостудії

Веб-сервіс для управління бронюваннями фотостудії з інтерактивним календарем.

## 🚀 Можливості

- **Інтерактивний календар** - перегляд місяця з виділенням днів з бронюваннями
- **Бронювання годин** - клієнти можуть обирати вільні години та залишати свої дані
- **Управління клієнтами** - автоматичне збереження інформації про клієнтів
- **Видалення бронювань** - можливість скасувати бронювання
- **Робочі години** - студія працює з 9:00 до 20:00
- **Перевірка доступності** - автоматична перевірка зайнятих годин
- **Responsive дизайн** - працює на всіх пристроях

## 📋 Технології

- **Backend**: FastAPI (Python)
- **Database**: PostgreSQL
- **ORM**: SQLAlchemy
- **Frontend**: Vanilla JavaScript, HTML5, CSS3
- **Deployment**: Docker + Docker Compose

## 🛠️ Встановлення

### Варіант 1: З Docker (рекомендовано)

1. **Клонуйте репозиторій або створіть структуру проекту**

2. **Створіть файл `.env`**:
```bash
cp .env.example .env
```

3. **Запустіть через Docker Compose**:
```bash
docker-compose up --build
```

4. **Відкрийте браузер**:
```
http://localhost:8000
```

### Варіант 2: Локально без Docker

1. **Встановіть PostgreSQL** (якщо не встановлено)

2. **Створіть базу даних**:
```sql
CREATE DATABASE photostudio_db;
CREATE USER photostudio WITH PASSWORD 'photostudio123';
GRANT ALL PRIVILEGES ON DATABASE photostudio_db TO photostudio;
```

3. **Встановіть Python залежності**:
```bash
pip install -r requirements.txt
```

4. **Налаштуйте змінні середовища** (створіть `.env`):
```bash
DATABASE_URL=postgresql://photostudio:photostudio123@localhost:5432/photostudio_db
```

5. **Запустіть сервер**:
```bash
uvicorn app.main:app --reload
```

6. **Відкрийте браузер**:
```
http://localhost:8000
```

## 📁 Структура проекту

```
photostudio-booking/
├── app/
│   ├── __init__.py
│   ├── main.py          # FastAPI додаток та endpoints
│   ├── models.py        # SQLAlchemy моделі
│   ├── schemas.py       # Pydantic схеми
│   └── database.py      # Налаштування бази даних
├── static/
│   └── index.html       # Frontend інтерфейс
├── docker-compose.yml   # Docker конфігурація
├── Dockerfile
├── requirements.txt
├── .env.example
└── README.md
```

## 🔌 API Endpoints

### Бронювання

- `POST /api/bookings/` - Створити нове бронювання
  ```json
  {
    "name": "Іван Іваненко",
    "phone": "+380501234567",
    "booking_date": "2025-11-10",
    "booking_hour": 14
  }
  ```

- `GET /api/bookings/` - Отримати всі бронювання (з фільтрами)
  - Query параметри: `start_date`, `end_date`

- `DELETE /api/bookings/{booking_id}` - Видалити бронювання

### Календар

- `GET /api/calendar/{year}/{month}` - Отримати статус всіх днів місяця
  - Приклад: `/api/calendar/2025/11`

- `GET /api/day/{booking_date}` - Отримати статус конкретного дня
  - Приклад: `/api/day/2025-11-10`

### Клієнти

- `GET /api/clients/` - Отримати всіх клієнтів
- `GET /api/clients/{client_id}` - Отримати клієнта по ID

## 💡 Використання

### Для адміністратора:

1. Відкрийте головну сторінку
2. Перегляньте календар - дні з бронюваннями виділені жовтим
3. Клікніть на день для перегляду деталей
4. Видаляйте бронювання кнопкою "Видалити"
5. Переглядайте сьогоднішні бронювання внизу сторінки

### Для клієнтів (майбутня функціональність):

1. Виберіть день в календарі
2. Оберіть вільну годину
3. Введіть ім'я та номер телефону
4. Натисніть "Забронювати"

## 🎨 Налаштування

### Зміна робочих годин

Відредагуйте змінну `WORK_HOURS` в файлі `app/main.py`:

```python
WORK_HOURS = list(range(9, 21))  # З 9:00 до 20:00
```

### Зміна кольорової схеми

Відредагуйте CSS змінні в файлі `static/index.html`:

```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

## 🔐 Безпека

Для продакшн використання:

1. Змініть паролі в `.env`
2. Додайте HTTPS
3. Додайте аутентифікацію адміністратора
4. Налаштуйте CORS правильно
5. Додайте rate limiting

## 📝 Майбутні покращення

- [ ] Аутентифікація користувачів
- [ ] Email/SMS нагадування
- [ ] Інтеграція з платіжними системами
- [ ] Особистий кабінет клієнта
- [ ] Статистика та звіти
- [ ] Експорт бронювань
- [ ] Multiple студії
- [ ] Різні тривалості сесій (не тільки 1 година)

## 🐛 Відомі проблеми

Немає на даний момент.

## 📧 Контакти

Якщо у вас є питання або пропозиції - створіть Issue.

## 📄 Ліцензія

MIT License - використовуйте як завгодно!

---

**Приємної роботи з системою бронювання! 📸✨**
