# 📸 Система Бронювання Фотостудії

Повноцінна система бронювання для фотостудій з інтеграцією Telegram бота, автоматичною обробкою платежів та вибором додаткових послуг.

## ✨ Можливості

### 🌐 Веб-інтерфейс
- **Інтерактивний календар** - Візуальний вибір дати та часу
- **Доступність в реальному часі** - Миттєве оновлення статусу бронювань
- **Адаптивний дизайн** - Відмінно працює на комп'ютерах та мобільних
- **Адмін-панель** - Управління бронюваннями та перегляд статистики

### 🤖 Telegram Бот
- **Автоматичне підтвердження бронювань** - Клієнти підтверджують через Telegram
- **Покроковий вибір послуг** - Інтерактивна анкета для додаткових послуг
- **Автоматичний розрахунок ціни** - Динамічне ціноутворення на основі вибору
- **Обробка платежів** - Інтегровані реквізити оплати та завантаження скріншотів
- **Сповіщення адміністратора** - Сповіщення про нові бронювання та оплати в реальному часі
- **Постійне меню** - Швидкий доступ до сайту та Instagram

### 💰 Система Додаткових Послуг
- **Кількість людей** - До 4 включено, +100 грн за кожну додаткову особу
- **Фотозони** - Світла, Темна або Обидві (+500 грн)
- **Тварини** - 1 включена, +100 грн за кожну додаткову
- **Фони** - Білий/Чорний/Червоний (+100 грн за кожен)
- **Відображення загальної суми** - Автоматичний розрахунок та підсумок

### 🔐 Безпека та Надійність
- **База даних PostgreSQL** - Надійне зберігання даних
- **Розгортання через Docker** - Легке налаштування та масштабування
- **Змінні середовища** - Безпечне управління конфігурацією
- **Готовність до SSL/HTTPS** - Захищені з'єднання

---

## 🛠️ Технології

- **Backend:** FastAPI (Python 3.11)
- **Бот:** python-telegram-bot
- **База даних:** PostgreSQL 15
- **Розгортання:** Docker + Docker Compose
- **Веб-сервер:** Nginx (для продакшну)
- **SSL:** Let's Encrypt (Certbot)

---

## 📋 Вимоги

- Python 3.11+
- Docker & Docker Compose
- PostgreSQL 15+ (або використовувати Docker)
- Токен Telegram Бота
- VPS/Сервер для продакшну

---

## 🚀 Швидкий Старт

### 1. Клонувати Репозиторій

```bash
git clone https://github.com/romanlenger/photostudio-booking.git
cd photostudio-booking
```

### 2. Створити Файл Середовища

Створити `.env` файл в кореневій директорії:

```bash
# База даних
DB_PASSWORD=ваш_надійний_пароль

# Telegram Бот
BOT_TOKEN=токен_бота_від_botfather
ADMIN_IDS=123456789,987654321

# URL адреси
WEBSITE_URL=http://localhost:8000
INSTAGRAM_URL=https://instagram.com/ваш_профіль

# Інформація про студію (опціонально)
STUDIO_RULES="Правила вашої студії тут"
```

### 3. Запустити через Docker

```bash
# Зібрати та запустити всі сервіси
docker-compose up -d

# Перевірити статус
docker-compose ps

# Переглянути логи
docker-compose logs -f
```

### 4. Ініціалізувати Базу Даних

```bash
# Виконати міграції
docker exec photostudio-booking-db-1 psql -U photostudio -d photostudio_db -f /migrations/001_initial.sql
docker exec photostudio-booking-db-1 psql -U photostudio -d photostudio_db -f /migrations/002_add_telegram.sql
docker exec photostudio-booking-db-1 psql -U photostudio -d photostudio_db -f /migrations/003_add_additional_services.sql
```

### 5. Отримати Доступ до Застосунку

- **Веб-сайт:** http://localhost:8000
- **Адмін-панель:** http://localhost:8000/admin.html
- **Telegram Бот:** Знайдіть свого бота в Telegram

---

## 📁 Структура Проекту

```
photostudio-booking/
├── app/                      # FastAPI застосунок
│   ├── __init__.py
│   ├── main.py              # Головний застосунок
│   ├── models.py            # Моделі бази даних
│   ├── database.py          # З'єднання з базою
│   └── routers/             # API маршрути
├── bot.py                    # Telegram бот
├── static/                   # Фронтенд файли
│   ├── index.html           # Сторінка бронювання
│   └── admin.html           # Адмін-панель
├── migrations/               # SQL міграції
│   ├── 001_initial.sql
│   ├── 002_add_telegram.sql
│   └── 003_add_additional_services.sql
├── docker-compose.yml        # Оркестрація Docker
├── Dockerfile               # Docker образ
├── requirements.txt         # Python залежності
├── .env                     # Змінні середовища (створити вручну)
└── README.md                # Цей файл
```

---

## 🔧 Конфігурація

### Змінні Середовища

| Змінна | Опис | Приклад |
|--------|------|---------|
| `DB_PASSWORD` | Пароль PostgreSQL | `supersecret123` |
| `BOT_TOKEN` | Токен Telegram бота | `123456:ABC-DEF...` |
| `ADMIN_IDS` | ID адміністраторів Telegram | `123456789,987654321` |
| `WEBSITE_URL` | URL веб-сайту | `https://ваш-домен.com` |
| `INSTAGRAM_URL` | Профіль Instagram | `https://instagram.com/студія` |
| `STUDIO_RULES` | Текст правил студії | `1. Прийти вчасно...` |

### Налаштування Telegram Бота

1. Напишіть [@BotFather](https://t.me/BotFather) в Telegram
2. Надішліть `/newbot` та слідуйте інструкціям
3. Скопіюйте токен бота
4. Додайте токен у файл `.env`
5. Отримайте свій Telegram ID від [@userinfobot](https://t.me/userinfobot)
6. Додайте свій ID до `ADMIN_IDS` у `.env`

---

## 💳 Налаштування Оплати

Відредагуйте реквізити оплати в `bot.py`:

```python
card_number = "5168757412345678"
card_display = "5168 7574 1234 5678"
```

Та в повідомленні оплати:

```python
payment = f"""💳 Реквізити:

<code>{card_display}</code>
Ім'я Отримувача

Сума: {price} грн
"""
```

---

## 🌐 Розгортання на Продакшні

### На VPS (Ubuntu 22.04)

#### 1. Встановити Docker

```bash
curl -fsSL https://get.docker.com | sh
```

#### 2. Клонувати та Налаштувати

```bash
git clone https://github.com/romanlenger/photostudio-booking.git
cd photostudio-booking
nano .env  # Додати вашу конфігурацію
```

#### 3. Запустити Сервіси

```bash
docker-compose up -d
```

#### 4. Налаштувати Nginx

```bash
sudo apt install nginx

# Створити конфіг
sudo nano /etc/nginx/sites-available/photostudio
```

Додати:

```nginx
server {
    listen 80;
    server_name ваш-домен.com www.ваш-домен.com;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

```bash
# Активувати конфіг
sudo ln -s /etc/nginx/sites-available/photostudio /etc/nginx/sites-enabled/
sudo rm /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl restart nginx
```

#### 5. Налаштувати SSL (HTTPS)

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d ваш-домен.com -d www.ваш-домен.com
```

---

## 🔄 Оновлення

Для оновлення застосунку на сервері:

```bash
# SSH на сервер
ssh root@ваш-сервер-ip

# Перейти до проекту
cd photostudio-booking

# Завантажити останні зміни
git pull

# Перезапустити сервіси
docker-compose restart bot web

# Або перезібрати якщо потрібно
docker-compose down
docker-compose build
docker-compose up -d
```

---

## 📊 Схема Бази Даних

### Таблиці

- **clients** - Інформація про клієнтів (ім'я, телефон, email)
- **bookings** - Записи бронювань з датами, часом та статусом
- **Поля додаткових послуг:**
  - `people_count` - Кількість людей
  - `zone_choice` - Обрана фотозона
  - `animals_count` - Кількість тварин
  - `background_choice` - Тип фону
  - `total_price` - Розрахована загальна ціна

---

## 🐛 Вирішення Проблем

### Бот не відповідає

```bash
# Перевірити логи бота
docker-compose logs -f bot

# Перезапустити бота
docker-compose restart bot
```

### Проблеми з підключенням до бази даних

```bash
# Перевірити логи бази даних
docker-compose logs -f db

# Перевірити чи працює база даних
docker-compose ps
```

### Порт вже використовується

```bash
# Перевірити що використовує порт 8000
sudo lsof -i :8000

# Завершити процес або змінити порт в docker-compose.yml
```

---

## 📝 API Endpoints

### Публічні Endpoints

- `GET /` - Сторінка бронювання
- `GET /admin.html` - Адмін-панель
- `GET /available_slots?date=YYYY-MM-DD` - Отримати доступні часові слоти
- `POST /book` - Створити нове бронювання

### Endpoints Бази Даних

- `GET /bookings` - Список всіх бронювань
- `GET /clients` - Список всіх клієнтів

---

## 🎨 Налаштування

### Змінити Назву Студії

Відредагувати `static/index.html` та `static/admin.html`

### Змінити Ціноутворення

Відредагувати `bot.py` у функції `calculate_price()`:

```python
def calculate_price(people, zone, animals, bg):
    price = 1000  # Базова ціна
    if people > 4: 
        price += (people - 4) * 100  # За особу
    if zone == 'both': 
        price += 500  # Обидві зони
    if animals > 1: 
        price += (animals - 1) * 100  # За тварину
    if bg != 'none': 
        price += 100  # Фон
    return price
```

### Додати Більше Послуг

1. Додати поля до бази даних в `app/models.py`
2. Створити SQL файл міграції
3. Додати питання в `bot.py`
4. Оновити функцію `calculate_price()`

---

## 📞 Підтримка

Для питань та проблем:
- Створити issue на GitHub
- Перевірити існуючу документацію
- Переглянути логи: `docker-compose logs`

---

## 📄 Ліцензія

Цей проект є приватним. Всі права захищені.

---

## 🙏 Подяки

Створено за допомогою:
- [FastAPI](https://fastapi.tiangolo.com/)
- [python-telegram-bot](https://python-telegram-bot.org/)
- [PostgreSQL](https://www.postgresql.org/)
- [Docker](https://www.docker.com/)

---

## 📈 Майбутні Функції

- [ ] Інтеграція онлайн-платежів
- [ ] Email сповіщення
- [ ] Експорт календаря (iCal)
- [ ] Багатомовна підтримка
- [ ] Історія бронювань для клієнтів
- [ ] Система відгуків

---

**Створено з ❤️ для фотостудій**
