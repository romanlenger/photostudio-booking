# 🚀 Швидкий Старт

## Найпростіший спосіб запустити проект

### 1. Переконайтеся що у вас встановлено:
- Docker Desktop (https://www.docker.com/products/docker-desktop)
- Або Docker + Docker Compose для Linux

### 2. Запуск проекту:

```bash
# Перейдіть в папку проекту
cd photostudio-booking

# Запустіть Docker Compose
docker-compose up --build
```

### 3. Відкрийте браузер:
```
http://localhost:8000
```

Готово! 🎉

---

## Що відбувається під капотом:

Docker автоматично:
- Створює PostgreSQL базу даних
- Встановлює всі Python залежності
- Запускає FastAPI сервер
- Створює таблиці в базі даних

---

## Корисні команди:

### Зупинити сервер:
```bash
docker-compose down
```

### Переглянути логи:
```bash
docker-compose logs -f
```

### Видалити все (включно з даними):
```bash
docker-compose down -v
```

### Перезапустити:
```bash
docker-compose restart
```

---

## Перевірка API:

Відкрийте в браузері:
```
http://localhost:8000/docs
```

Це інтерактивна документація API (Swagger UI) де можна тестувати всі endpoints!

---

## Структура бази даних:

**Таблиця clients:**
- id (автоматичний)
- name (ім'я клієнта)
- phone (номер телефону, унікальний)
- created_at (дата створення)

**Таблиця bookings:**
- id (автоматичний)
- client_id (зв'язок з клієнтом)
- booking_date (дата бронювання)
- booking_hour (година від 9 до 20)
- created_at (дата створення)

---

## Що можна робити через інтерфейс:

✅ Переглядати календар з виділенням зайнятих днів
✅ Клікати на день для перегляду деталей
✅ Бронювати вільні години
✅ Видаляти бронювання
✅ Бачити всі сьогоднішні бронювання

---

## Налаштування робочих годин:

За замовчуванням студія працює **з 9:00 до 20:00**.

Щоб змінити, відредагуйте файл `app/main.py`:

```python
WORK_HOURS = list(range(9, 21))  # змініть цифри
```

Наприклад, для роботи з 10:00 до 18:00:
```python
WORK_HOURS = list(range(10, 18))
```

---

## Troubleshooting:

### Порт 8000 вже зайнятий?
Змініть порт в `docker-compose.yml`:
```yaml
ports:
  - "8001:8000"  # використовуйте 8001 замість 8000
```

### База даних не створюється?
Видаліть старі дані та спробуйте знову:
```bash
docker-compose down -v
docker-compose up --build
```

### Зміни в коді не застосовуються?
Docker автоматично перезавантажується, але якщо щось не працює:
```bash
docker-compose restart web
```

---

**Якщо виникли інші проблеми - дивіться логи:**
```bash
docker-compose logs -f web
```

Успіхів! 🚀
