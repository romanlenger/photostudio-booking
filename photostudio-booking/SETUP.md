# 🚀 ІНСТРУКЦІЯ З ВСТАНОВЛЕННЯ

## 📋 ЩО ТРЕБА ЗРОБИТИ:

### 1️⃣ Створити Telegram бота

1. Відкрий Telegram і знайди [@BotFather](https://t.me/BotFather)
2. Надішли команду `/newbot`
3. Вкажи назву бота (наприклад: "CLIQUE Booking")
4. Вкажи username бота (наприклад: "clique_booking_bot")
5. Збережи **токен** який дасть BotFather (типу `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)
6. Збережи **username** бота (типу `clique_booking_bot`)

---

### 2️⃣ Дізнатися свій Telegram ID

1. Відкрий [@userinfobot](https://t.me/userinfobot)
2. Надішли `/start`
3. Бот покаже твій ID (наприклад: `123456789`)
4. Якщо адмінів декілька - збери всі ID через кому

---

### 3️⃣ Налаштувати проект

```bash
# 1. Скопіюй всі файли в папку
cd ~/photostudio-booking

# 2. Створи .env файл
cp .env.example .env

# 3. Відредагуй .env
nano .env
```

**Заповни .env:**
```bash
# Токен від BotFather
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Username бота (БЕЗ @)
BOT_USERNAME=clique_booking_bot

# Твій Telegram ID (або декілька через кому)
ADMIN_IDS=123456789,987654321

# Правила студії (можна змінити пізніше)
STUDIO_RULES=📋 Правила фотостудії CLIQUE:\n\n1. ...\n2. ...

# Реквізити (можна змінити пізніше)
PAYMENT_DETAILS=💳 Картка: 1234 5678 9012 3456
```

---

### 4️⃣ Додати HTML файли

```bash
# Створи папку static
mkdir -p static

# Скопіюй туди index.html та admin.html
cp index-template.html static/index.html
cp admin.html static/admin.html
```

---

### 5️⃣ Запустити

```bash
# Запусти Docker
docker-compose up -d --build

# Перевір логи
docker-compose logs -f
```

---

## ✅ ПЕРЕВІРКА:

### 1. Перевір веб-сайт:
```
http://192.168.88.26:8000
```

### 2. Перевір бота:
```
1. Знайди бота в Telegram: @clique_booking_bot
2. Надішли /start
3. Маєш побачити привітання для адміна
```

### 3. Створи тестове бронювання:
```
1. Відкрий сайт
2. Обери дату і час
3. Заповни форму
4. Натисни "Забронювати"
5. Маєш перейти в Telegram
6. Побач правила + кнопки
```

---

## 🎯 FLOW БРОНЮВАННЯ:

```
КЛІЄНТ:
1. Сайт → Вибір дати/часу
2. Форма → Ім'я + телефон
3. "Забронювати" → Перехід в Telegram
4. Telegram → Правила + кнопки
5. "Підтвердити" → Реквізити
6. Оплата → Скріншот квитанції

АДМІН:
1. Отримав сповіщення: "Нове бронювання"
2. Отримав сповіщення: "Підтверджено"
3. Отримав скріншот оплати
4. Перевіряє оплату
```

---

## 📱 КОМАНДИ БОТА:

### Для клієнтів:
- `/start` - Привітання
- `/help` - Довідка

### Для адмінів:
- `/start` - Статус адміна
- Отримують всі сповіщення автоматично

---

## 🔧 НАЛАШТУВАННЯ ПРАВИЛ:

Відредагуй `.env`:

```bash
STUDIO_RULES=📋 Правила фотостудії CLIQUE:\n\n1. Приходьте вчасно - час бронювання фіксований\n2. Максимальна кількість людей - згідно з пакетом\n3. За пошкодження обладнання - відповідальність клієнта\n4. Заборонено курити в студії\n5. Домашні тварини - тільки після узгодження\n\n⚠️ При скасуванні менше ніж за 24 год - передоплата не повертається
```

Перезапусти:
```bash
docker-compose restart bot
```

---

## 💳 НАЛАШТУВАННЯ РЕКВІЗИТІВ:

Відредагуй `.env`:

```bash
PAYMENT_DETAILS=💳 Реквізити для оплати:\n\nКартка ПриватБанк:\n5168 7574 1234 5678\n\nОтримувач: Іван Петренко\n\nСума: 500 грн\n\nПризначення платежу: Бронювання фотостудії {date} {time}\n\n📸 Після оплати надішліть скріншот квитанції в цей чат.
```

**Важливо:** `{date}` та `{time}` замінюються автоматично!

Перезапусти:
```bash
docker-compose restart bot
```

---

## 🐛 TROUBLESHOOTING:

### Бот не відповідає:
```bash
# Перевір логи
docker-compose logs bot

# Перевір токен в .env
cat .env | grep BOT_TOKEN
```

### Не переходить в Telegram:
```bash
# Перевір BOT_USERNAME в .env
cat .env | grep BOT_USERNAME

# Має бути БЕЗ @
```

### Адмін не отримує сповіщення:
```bash
# Перевір ADMIN_IDS
cat .env | grep ADMIN_IDS

# Має бути твій Telegram ID
```

### База даних не працює:
```bash
# Перевір права
ls -la bookings.db

# Створи заново
rm bookings.db
docker-compose restart web
```

---

## 📊 СТАТУСИ БРОНЮВАНЬ:

- `pending` - Створено, очікує підтвердження
- `confirmed` - Підтверджено, очікує оплату
- `paid` - Оплачено (після скріншота)
- `cancelled` - Скасовано

---

## 🔐 БЕЗПЕКА:

1. ✅ Не показуй файл `.env` нікому
2. ✅ Додай `.env` в `.gitignore`
3. ✅ Змінюй `ADMIN_IDS` тільки для перевірених людей
4. ✅ Backup БД регулярно: `cp bookings.db bookings_backup.db`

---

## 📞 ПІДТРИМКА:

Якщо щось не працює - перевір:
1. Docker запущений: `docker ps`
2. Логи: `docker-compose logs -f`
3. .env правильний: `cat .env`
4. Порт 8000 вільний: `netstat -tulpn | grep 8000`

---

**ВСЕ ГОТОВО! ЗАПУСКАЙ! 🚀✨**
